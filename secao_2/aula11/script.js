// alert('Alguma caixinha')

// window.confirm('Deseja realmente apagar?') // Confirmação

// window.prompt('Digite o seu nome: ') // Input

const a = window.prompt('Digite algum numero');
const b = window.prompt('Digite outro numero');
const resultado = Number(a) + Number(b);

alert(`O resultado da soma foi ${resultado}`)